<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MeasureBook.MainPage"
             xmlns:model="clr-namespace:MeasureBook.Model">

    <CollectionView>
        <CollectionView.ItemsSource>
            <x:Array Type="{x:Type model:ClientModel}">
                
                <model:ClientModel
                    Name="Marvin" />
                <model:ClientModel
                    Name="Marvin" />
                <model:ClientModel
                    Name="Marvin" />
                
            </x:Array>
            
        </CollectionView.ItemsSource>
        
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:ClientModel">
                <Label VerticalOptions="Center">
                    <Label.Text>
                        <MultiBinding StringFormat="{}{0} | {1}" >
                            <Binding Path="Name" />
                            <Binding Path="Id" />
                        </MultiBinding>
                    </Label.Text>
                </Label>
                
                
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
			 
    
 
</ContentPage>
